# pnpm reads its settings from pnpm-workspace.yaml, including `engineStrict`.
# This file exists for the other direction: someone reaching for `npm install`
# out of habit. npm's `engines` check is advisory by default, and the Node
# ceiling in package.json is the part worth enforcing on that path too.
engine-strict=true
