# Fail loudly when the toolchain is wrong instead of silently resolving a
# different dependency tree. The `engines` field in package.json is advisory by
# default: an out-of-date npm will happily install and write a package-lock.json
# that nobody else reproduces.
engine-strict=true
